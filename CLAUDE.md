# BRLa Pipeline Project

## conda env
- Always run Python commands via `conda run -n env-brla` — the conda environment has all dependencies installed.

## Pipeline state
Read `notes/state.md` before resuming pipeline work — it has the current-state marker, module status, and links to all other state files.